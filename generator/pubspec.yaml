name: generator
description: Used to generate code automatically
version: 1.0.0

environment:
  sdk: ">=3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  archive:
  dio:
  json_annotation:
  path:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
